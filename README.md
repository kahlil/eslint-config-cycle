# Cycle's ESLint configuration

## Install

```sh
npm install --save-dev eslint-config-cycle
```

## Usage

In your project's `.eslintrc`, use the `extends` feature:

```js
{
  "extends": "eslint-config-cycle"
}
```
